export type RegisterResponse = {
    first_name: string,
    last_name: string,
    full_name: string
    email: string,
    password: string,
    access_token: string,
    refresh_token: string
    message: string,
    id?: number
};

export type ProfileResponse = {
    
};
